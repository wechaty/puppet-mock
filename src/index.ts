import {
  VERSION,
  log,
}             from './config'

import { PuppetMock } from './puppet-mock'

import {
  Mocker,
  MockerBehavior,
  SimpleBehavior,
}                 from './mocker'

export default PuppetMock
export {
  VERSION,
  log,
  Mocker,
  MockerBehavior,
  SimpleBehavior,
  PuppetMock,
}
