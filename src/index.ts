import {
  VERSION,
  log,
}             from './config'

import { PuppetMock } from './puppet-mock'

import {
  Mocker,
  MockEnvironment,
  SimpleEnvironment,
}                 from './mocker'

export default PuppetMock
export {
  VERSION,
  log,
  Mocker,
  MockEnvironment,
  SimpleEnvironment,
  PuppetMock,
}
